Optimize Portfolio
====
## Requirement

dockerをインストール
$ brew install docker

ターミナルでdocker-composeコマンドを実行
＄ docker-compose up --build -d

#上記のコマンドを打ったのちローカルホストを起動
localhost:1111

#herokuでデプロイするとき
cd src にする